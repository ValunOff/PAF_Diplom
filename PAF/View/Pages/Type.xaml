<Page x:Class="PAF.View.Pages.Type"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <DataGrid ItemsSource="{Binding Types, Mode=OneWay}" SelectionMode="Single"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Внести изменения" Command="{Binding SaveChangesCommand}" Style="{StaticResource ViewButtonStule}" Grid.Column="1"/>
            <Button Content="Добавить" Command="{Binding AddCommand}" Style="{StaticResource ViewButtonStule}" Grid.Column="3"/>
            <Button Content="Обновить" Command="{Binding UpdateCommand}" Style="{StaticResource ViewButtonStule}" Grid.Column="5"/>
        </StackPanel>
        
    </Grid>
</Page>
